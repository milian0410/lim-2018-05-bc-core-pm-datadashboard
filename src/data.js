




/* window.data = 
{
  computeUsersStats:(users, progress, courses)=>
  { 
       let name="";
       for(let i=0; i<users.length; i++)
        {
          name =(users[i].name);
          progress=(users[i].id);
        }  
        return name    
  },
    
} */
